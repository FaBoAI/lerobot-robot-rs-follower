# v0.0.18

## 追補2 (2026-08-29 夜): かさ持ち上げ時のトルク暴走対策

かさを掴んで持ち上げた瞬間に 10.1A / 5.27N·m (MIT 飽和) を記録し、ファーム
保護で切断 → 温度 30→64°C/1.5s。**モータ側 limit_torque(3.5)/limit_cur(6.5A)
は MIT (運控) モードでは出力を制限しない**ことが確定 (起動時の書込・検証は
通るが、飽和値まで実測)。ソフト側で常時保証する方式に変更:

- **閉じ方向の位置誤差クランプ (常時)**: target ≤ pos + max_torque/kp。
  kp 項の実効トルクが max_torque を超えられない、実際に機能する上限。
- **ラッチ中のバックドライブ追従**: 固定 trip_target のままだと外力で指が
  押し開かれた際に位置誤差が育ち 8-10A が持続した → 現在位置より閉じ側へは
  指令しない。
- **グリッパのみ kd 1.0→0.4** (gripper_soft_gains): バックドライブ速度×kd の
  減衰トルクが暴走の増幅源。kp=20 は据え置き。
- 自動解除は実機で機能を確認 (トリップ後 ~8 秒で自動復帰)。

## 追補 (2026-08-29 夕): 「掴む前にロック」対策

実機で「接触前・電流ほぼゼロ (0.04-0.06A)・冷間」でもモータがフォルトフラグを
報告して即ラッチする事象が発生 (14:42 セッション開始 3 秒でトリップ)。
このゼロ電流フォルトは設定変更前 (13:45-13:51) にも出ており、13:29 以降の
堵転フォルト連発の後遺症 (モータ保護状態の蓄積) が疑われる。→ アーム電源の
再投入を推奨。ソフト側は以下を変更:

- 天井を巻き戻し: `limit_torque` 4.2→3.5 N·m (突入スパイク抑制)、
  `limit_cur` 8.0→6.5 A、保持ピーク 3.8→3.4 N·m、ランプ 8→5 N·m/s。
  ソフト電流リミットをファーム堵転保護 (実測 4.8-5.2A 発火) より下へ:
  soft 5.8 / hard 6.5 / release 5.0 A。
- **ラッチ自動解除**: `latch_until_open=false` 構成では、クールダウン後に
  健全ステータスが戻れば開き操作なしで解除 (従来は開き指令が必須で、
  閉じ続けるポリシー相手では手が死んだままだった)。
- **フォルト自動復旧**: fault-clear + 再イネーブル試行も開き操作待ちを撤廃
  (`latch_until_open=false` 時のみ・1 秒に 1 回)。
- オフライン検証: フォルト→フィードバック断→復旧試行→自動解除→閉じ再開を
  シミュレーションで確認 (sim_gripper_autounlatch.py PASS)。


グリッパーの保持圧力アップ + 「ロックしない」対策 (2026-08-29)。

## 背景 (実測)

- RS05 firmware の堵転保護 (locked-rotor protection) は、`4.8-5.2 A` (≈3.2 N·m) の
  ストール電流が **1.5-2.5 秒連続** しただけで発火する
  (`overcurrent=True, hard_fault=True` → フィードバックごと死んで「手が死ぬ」)。
  13:41 の計測では 5.2 A が 5.5 秒続いても発火しない例があり、微小な動きで
  firmware 側タイマーがリセットされると考えられる。
- 保持中の温度上昇は約 `6 °C/s` (5.2 A 保持で 30→65 °C / 5.5 s)。

## 変更

- **堵転保護リリーフ** (`gripper_stall_relief_*`) を追加: 電流が
  `gripper_stall_relief_current_a` (4.2 A) を超えて `high_s` (0.7 s) 連続したら、
  保持トルクを `rest_s` (0.3 s) だけ `rest_torque` (1.8 N·m) へ落として firmware の
  ストールタイマーをリセットし、その後 8 N·m/s で再ランプする。
  ディップ中も lock_target は接触点に残るため指は開かない。
  電流が読めない間はトルク (`gripper_stall_relief_torque_nm`) で代替判定。
- **温度リリーフ** (`gripper_temp_soft_limit_c` / `gripper_temp_release_c`) を追加:
  報告温度 70 °C で保持を rest トルクへクランプし、60 °C まで冷えたら復帰。
- **保持ピークを引き上げ**: `gripper_hold_max_torque_nm: 3.8` を新設
  (モータ側 `limit_torque` は 4.2 N·m へ)。旧実効 ~3.2 N·m から約 +19%。
  追従して soft/hard/release/current-soft/max_hold_error を調整
  (4.0 / 4.6 / 3.50 N·m、7.4 A、0.20 rad)。
- ランプ 3→8 N·m/s、バックオフ 2.5→5 N·m/s (ディップ後の復帰を速く)。

## 検証

- オフラインシミュレーション (50 Hz ループ / 20 Hz フィードバック /
  firmware 堵転モデル 4.5 A × 1.5 s): ピーク 3.80 N·m、リリーフディップ
  10 回 / 11.5 s、4.5 A 超の最長連続 0.74 s、firmware トリップなし、
  指令ターゲットは接触点より開かない、温度リリーフ 75 °C で作動。
- 実機での要確認: ディップ間隔 (`high_s`) を伸ばしても発火しないか、
  重量物がディップ 0.3 s で滑らないか。滑る場合は `rest_torque` を 2.2 へ。
